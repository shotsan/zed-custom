# Generated from xtask::workflows::extensions::run_tests within the zed_custom repository.
# Rebuild with `cargo xtask workflows`.
name: extensions::run_tests
on:
  pull_request:
    branches:
    - '**'
  push:
    branches:
    - main
jobs:
  call_extension_tests:
    permissions:
      contents: read
    uses: shotsan/zed-custom/.github/workflows/extension_tests.yml@main
concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}-${{ github.ref_name == 'main' && github.sha || 'anysha' }}pr
  cancel-in-progress: true
defaults:
  run:
    shell: bash -euxo pipefail {0}
