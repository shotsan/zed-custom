import{_ as s,o as t,c as i,ag as a}from"./chunks/framework.BSwSpTvy.js";const k=JSON.parse('{"title":"🔬 Internals: Tree-sitter, AST, LSP & Code Indexing","description":"","frontmatter":{},"headers":[],"relativePath":"features/internals.md","filePath":"features/internals.md"}'),n={name:"features/internals.md"};function r(l,e,o,h,d,p){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="🔬-internals-tree-sitter-ast-lsp-code-indexing" tabindex="-1">🔬 Internals: Tree-sitter, AST, LSP &amp; Code Indexing <a class="header-anchor" href="#🔬-internals-tree-sitter-ast-lsp-code-indexing" aria-label="Permalink to &quot;🔬 Internals: Tree-sitter, AST, LSP &amp; Code Indexing&quot;">​</a></h1><p>This page explains the four technologies that work together to give the agent its understanding of your code. Each one operates at a different layer and solves a different problem.</p><hr><h2 id="abstract-syntax-tree-ast" tabindex="-1">Abstract Syntax Tree (AST) <a class="header-anchor" href="#abstract-syntax-tree-ast" aria-label="Permalink to &quot;Abstract Syntax Tree (AST)&quot;">​</a></h2><p>An <strong>Abstract Syntax Tree</strong> is the data structure that results from parsing source code. Instead of treating a file as a flat string of characters, a parser reads the code and builds a tree where each node represents a meaningful unit of the language — a function definition, a block, an expression, an identifier.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// Source text</span></span>
<span class="line"><span>fn add(a: i32, b: i32) -&gt; i32 {</span></span>
<span class="line"><span>    a + b</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// What the AST looks like (simplified)</span></span>
<span class="line"><span>function_item</span></span>
<span class="line"><span>  name: identifier &quot;add&quot;</span></span>
<span class="line"><span>  parameters: parameters</span></span>
<span class="line"><span>    parameter: identifier &quot;a&quot; type: primitive_type &quot;i32&quot;</span></span>
<span class="line"><span>    parameter: identifier &quot;b&quot; type: primitive_type &quot;i32&quot;</span></span>
<span class="line"><span>  return_type: primitive_type &quot;i32&quot;</span></span>
<span class="line"><span>  body: block</span></span>
<span class="line"><span>    binary_expression</span></span>
<span class="line"><span>      left: identifier &quot;a&quot;</span></span>
<span class="line"><span>      right: identifier &quot;b&quot;</span></span></code></pre></div><p>The AST <strong>removes all syntactic noise</strong> (whitespace, comments, parentheses used purely for grouping) and retains only the semantic structure of the code. All the other tools described below — Tree-sitter, and LSP — are built on top of some form of this structure.</p><hr><h2 id="tree-sitter" tabindex="-1">Tree-sitter <a class="header-anchor" href="#tree-sitter" aria-label="Permalink to &quot;Tree-sitter&quot;">​</a></h2><p><strong>Tree-sitter</strong> is a fast, incremental parser that builds an AST from source text. It is embedded directly into Zed as a Rust library (<code>crates/language/src/language.rs</code> exports <code>tree_sitter::Node</code>, <code>Tree</code>, <code>Parser</code>, etc.).</p><h3 id="what-it-does" tabindex="-1">What it does <a class="header-anchor" href="#what-it-does" aria-label="Permalink to &quot;What it does&quot;">​</a></h3><p>Tree-sitter parses a file into an AST using a <strong>grammar</strong> compiled for each language (e.g., <code>tree_sitter_rust::LANGUAGE</code>, <code>tree_sitter_python::LANGUAGE</code>). It is designed for editors: it is incremental, meaning it only re-parses the parts of a file that changed, not the whole document, making it fast enough to run on every keystroke.</p><p>Tree-sitter does <strong>not</strong> know anything about your project. It parses one file at a time, in isolation, with no knowledge of imports, types, or external definitions. It understands syntax — not semantics.</p><h3 id="where-this-repo-uses-it" tabindex="-1">Where this repo uses it <a class="header-anchor" href="#where-this-repo-uses-it" aria-label="Permalink to &quot;Where this repo uses it&quot;">​</a></h3><p>Every open buffer in Zed maintains a <code>TreeSitterData</code> struct (see <code>crates/language/src/buffer.rs</code>). This gives each buffer a live, always-current syntax tree.</p><p>The specific place this matters for the agent is <strong>file outline generation</strong>, implemented in <code>crates/agent/src/outline.rs</code>:</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// From outline.rs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AUTO_OUTLINE_SIZE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> usize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 16384</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 16 KB</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// When a file is larger than 16 KB, the agent reads the tree-sitter</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// outline instead of the full file content.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> outline_items </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> buffer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">read_with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cx, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">buffer, _</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> snapshot </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> buffer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">snapshot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    snapshot</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">outline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">None</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">items  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &lt;-- this calls into tree-sitter</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">into_iter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">item</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> item</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">to_point</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">snapshot))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">collect</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;_&gt;&gt;()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>When the agent opens a file over 16 KB, it asks the buffer for its <strong>outline</strong> — a compact, tree-sitter-derived list of top-level symbols (functions, structs, impls, classes, etc.) with their line ranges. This is sent to the model instead of the full file content, keeping context usage bounded on large files.</p><p>If a file has no parseable tree-sitter outline (e.g., a plain text file or an unsupported language), the agent falls back to the first 1 KB of raw content.</p><h3 id="what-tree-sitter-cannot-do" tabindex="-1">What Tree-sitter cannot do <a class="header-anchor" href="#what-tree-sitter-cannot-do" aria-label="Permalink to &quot;What Tree-sitter cannot do&quot;">​</a></h3><ul><li>It cannot resolve a type reference across files.</li><li>It cannot tell you where a function is defined if it&#39;s imported from another module.</li><li>It cannot understand generics, trait bounds, or type inference.</li></ul><p>For those capabilities, you need a Language Server.</p><hr><h2 id="language-server-protocol-lsp" tabindex="-1">Language Server Protocol (LSP) <a class="header-anchor" href="#language-server-protocol-lsp" aria-label="Permalink to &quot;Language Server Protocol (LSP)&quot;">​</a></h2><p><strong>LSP</strong> is a standard JSON-RPC protocol that defines how an editor communicates with a language-specific analysis tool (a &quot;language server&quot;). The language server runs as a separate process alongside the editor. It has full knowledge of your project: it understands imports, resolves types, knows where every symbol is defined, and can answer queries like &quot;find all references to this function.&quot;</p><p>Common language servers this repo works with:</p><table tabindex="0"><thead><tr><th>Language</th><th>Server</th></tr></thead><tbody><tr><td>Rust</td><td><code>rust-analyzer</code></td></tr><tr><td>Python</td><td><code>pyright</code></td></tr><tr><td>Go</td><td><code>gopls</code></td></tr><tr><td>TypeScript/JavaScript</td><td><code>typescript-language-server</code></td></tr><tr><td>C/C++</td><td><code>clangd</code></td></tr></tbody></table><h3 id="how-the-protocol-works" tabindex="-1">How the protocol works <a class="header-anchor" href="#how-the-protocol-works" aria-label="Permalink to &quot;How the protocol works&quot;">​</a></h3><p>Language servers are <strong>started automatically when you open a file</strong> of the corresponding language — not when you open the project folder. The trigger is <code>register_buffer_with_language_servers()</code> inside <code>crates/project/src/lsp_store.rs</code>, which runs every time a buffer is created. It checks what language the file is, walks the LSP tree to determine which servers should handle it, and calls <code>start_language_server()</code> for any server not yet running.</p><p>A newly started server goes through a <code>Starting</code> state while it initializes (downloading index, reading <code>Cargo.lock</code>, etc.), then transitions to <code>Running</code>. Zed already maintains these persistent connections for the open project — the agent tools simply reuse them.</p><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p><code>project.symbols()</code> only queries servers in the <code>Running</code> state. A server that is still initializing is <strong>silently skipped</strong>. If you call <code>query_context</code> immediately after opening a file in a large Rust project, <code>rust-analyzer</code> may still be indexing and return no results — even though it will work correctly once initialization completes.</p></div><p>The LSP requests this fork uses for agent tools are:</p><table tabindex="0"><thead><tr><th>LSP Request</th><th>Tool</th><th>Description</th></tr></thead><tbody><tr><td><code>workspace/symbol</code></td><td><code>query_context</code></td><td>Fuzzy symbol search across the whole project</td></tr><tr><td><code>textDocument/definition</code></td><td><code>lsp_get_definition</code></td><td>Jump-to-definition at a position</td></tr><tr><td><code>textDocument/references</code></td><td><code>lsp_find_references</code></td><td>Find all call sites/uses of a symbol</td></tr><tr><td><code>textDocument/implementation</code></td><td><code>lsp_get_implementations</code></td><td>Find all types implementing a trait/interface</td></tr></tbody></table><h3 id="query-context-—-workspace-symbol-search" tabindex="-1"><code>query_context</code> — workspace symbol search <a class="header-anchor" href="#query-context-—-workspace-symbol-search" aria-label="Permalink to &quot;\`query_context\` — workspace symbol search&quot;">​</a></h3><p>The primary context tool calls <code>project.symbols(&amp;query, cx)</code>, which sends a <code>workspace/symbol</code> LSP request to all active language servers with your query string. The server does a fuzzy match against its own symbol index (which it maintains from compiling/analyzing the full project) and returns matches with their file path, kind, and line range.</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// From context_tool.rs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Symbol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    project</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cx, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">project, cx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        project</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">symbols</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">query, cx)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// LSP workspace/symbol request</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> symbols </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.await?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Results are capped at 20 symbols to avoid overflowing the model&#39;s context window.</p><h3 id="lsp-get-definition-lsp-find-references-lsp-get-implementations" tabindex="-1"><code>lsp_get_definition</code>, <code>lsp_find_references</code>, <code>lsp_get_implementations</code> <a class="header-anchor" href="#lsp-get-definition-lsp-find-references-lsp-get-implementations" aria-label="Permalink to &quot;\`lsp_get_definition\`, \`lsp_find_references\`, \`lsp_get_implementations\`&quot;">​</a></h3><p>These three tools in <code>crates/agent/src/tools/lsp_tools.rs</code> take a <strong>file path + line + column</strong> as input, open that buffer if it isn&#39;t open yet, resolve the position to a <code>PointUtf16</code>, and forward a targeted LSP request (<code>definition</code>, <code>references</code>, or <code>implementation</code>) to the relevant language server. Each returns a list of file paths with line and column positions.</p><p>All three are cancellable — if you click &quot;Stop&quot; in the agent panel mid-tool, the LSP request is abandoned via <code>event_stream.cancelled_by_user()</code>.</p><hr><h2 id="code-indexing" tabindex="-1">Code Indexing <a class="header-anchor" href="#code-indexing" aria-label="Permalink to &quot;Code Indexing&quot;">​</a></h2><p>This repo contains two separate symbol indexing mechanisms. Understanding which one is actually active matters.</p><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p><strong>There is no active project-wide indexer inside Zed itself.</strong> The only project-wide index that exists lives inside the language server process (e.g., <code>rust-analyzer</code>, <code>gopls</code>). If no language server is running for your language, the agent has no way to search symbols across the project.</p></div><h3 id="_1-the-semanticindex-regex-based-currently-dormant" tabindex="-1">1. The <code>SemanticIndex</code> (regex-based, currently dormant) <a class="header-anchor" href="#_1-the-semanticindex-regex-based-currently-dormant" aria-label="Permalink to &quot;1. The \`SemanticIndex\` (regex-based, currently dormant)&quot;">​</a></h3><p><code>crates/agent/src/semantic_search.rs</code> defines a <code>SemanticIndex</code> struct that stores a <code>HashMap&lt;String, Vec&lt;SymbolLocation&gt;&gt;</code>. Its <code>build()</code> method walks the project&#39;s worktrees, finds supported source files (<code>.rs</code>, <code>.py</code>, <code>.js</code>, <code>.ts</code>, <code>.go</code>, <code>.java</code>, <code>.cs</code>, <code>.cpp</code>, etc.), reads each one from disk, and extracts symbol names using <strong>regex patterns</strong> — not tree-sitter, not LSP.</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Python example from semantic_search.rs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> function_pattern </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Regex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">r&quot;(?m)^\\s*def\\s+(\\w+)\\s*\\(&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> class_pattern    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Regex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">r&quot;(?m)^\\s*class\\s+(\\w+)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Each language has its own set of regex patterns. The result is a flat in-memory map from symbol name → list of locations that can be queried for prefix and substring matches.</p><p><strong>However, this index is deliberately not running.</strong> In <code>agent.rs</code>:</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// agent.rs — NativeAgent::new()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> semantic_index </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Arc</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parking_lot</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RwLock</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SemanticIndex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maintain_semantic_index </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Task</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ready</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ok</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(()));  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &lt;-- no-op, nothing is scheduled</span></span></code></pre></div><p>The <code>SemanticIndex</code> struct is instantiated (and its <code>symbol_count()</code> is exposed in the status panel), but the background task that would call <code>SemanticIndex::build()</code> and keep it updated is replaced with a task that immediately resolves to <code>Ok(())</code>. <strong>The index is always empty in normal operation.</strong></p><p>This was a deliberate trade-off: the regex indexer was expensive to maintain (CPU, disk I/O on large projects) and less accurate than LSP. It is kept in the codebase but disabled in favour of the LSP path.</p><h3 id="what-happens-when-no-language-server-is-running" tabindex="-1">What happens when no language server is running? <a class="header-anchor" href="#what-happens-when-no-language-server-is-running" aria-label="Permalink to &quot;What happens when no language server is running?&quot;">​</a></h3><p>Because the <code>SemanticIndex</code> is disabled and tree-sitter only covers open files, <strong>there is no fallback project-wide symbol search</strong> if no language server is active for your language. Concretely:</p><ul><li><code>query_context</code> calls <code>project.symbols(&amp;query, cx)</code> — this forwards the request to all active language servers. With no servers, the result is an empty <code>Vec&lt;Symbol&gt;</code>, and the tool returns <code>&quot;No symbols found matching &#39;...&#39;&quot;</code>.</li><li><code>lsp_get_definition</code>, <code>lsp_find_references</code>, and <code>lsp_get_implementations</code> all require an open buffer at a specific position and an active language server connection. With no server, they will fail or return no results.</li><li>Tree-sitter outlines (via <code>read_file</code>) still work for any open file over 16 KB regardless of LSP status, since they only depend on a grammar being loaded.</li></ul><p>The practical consequence: for languages with good LSP support (Rust, Go, Python, TypeScript, C/C++), the agent has full project-wide awareness. For languages without a configured language server, the agent is limited to reading individual files the model explicitly asks to open.</p><h3 id="_2-tree-sitter-file-outlines-active-per-file" tabindex="-1">2. Tree-sitter File Outlines (active, per-file) <a class="header-anchor" href="#_2-tree-sitter-file-outlines-active-per-file" aria-label="Permalink to &quot;2. Tree-sitter File Outlines (active, per-file)&quot;">​</a></h3><p>As described in the Tree-sitter section, every open buffer maintains its own tree-sitter parse tree. This is <strong>not</strong> a project-wide index — it only covers files that are currently open as buffers. Its output is used exclusively by the <code>read_file</code> tool flow in <code>outline.rs</code> to avoid flooding the model&#39;s context with a full large file.</p><h3 id="_3-lsp-workspace-symbol-index-active-maintained-by-language-server" tabindex="-1">3. LSP Workspace Symbol Index (active, maintained by language server) <a class="header-anchor" href="#_3-lsp-workspace-symbol-index-active-maintained-by-language-server" aria-label="Permalink to &quot;3. LSP Workspace Symbol Index (active, maintained by language server)&quot;">​</a></h3><p>The language server itself maintains a full project-wide symbol index as part of its normal operation. When <code>rust-analyzer</code> starts, it indexes your entire Rust workspace. When <code>gopls</code> starts, it indexes all Go packages. This index is updated incrementally as you edit files.</p><p>This is the index the agent actually uses via <code>project.symbols()</code>. It is never built or managed by Zed or this fork — it lives entirely inside the language server process.</p><hr><h2 id="summary-which-technology-answers-which-question" tabindex="-1">Summary: Which Technology Answers Which Question <a class="header-anchor" href="#summary-which-technology-answers-which-question" aria-label="Permalink to &quot;Summary: Which Technology Answers Which Question&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Question</th><th>Technology</th><th>Active?</th></tr></thead><tbody><tr><td>Syntax highlight this token</td><td>Tree-sitter</td><td>✅ Always</td></tr><tr><td>Outline of a large open file</td><td>Tree-sitter outline</td><td>✅ When open file &gt; 16 KB</td></tr><tr><td>Search symbols across project</td><td>LSP <code>workspace/symbol</code></td><td>✅ After a file of that language is opened and server is <code>Running</code></td></tr><tr><td>Jump to definition at a position</td><td>LSP <code>textDocument/definition</code></td><td>✅ After a file of that language is opened and server is <code>Running</code></td></tr><tr><td>Find all uses of a symbol</td><td>LSP <code>textDocument/references</code></td><td>✅ After a file of that language is opened and server is <code>Running</code></td></tr><tr><td>Find trait/interface implementations</td><td>LSP <code>textDocument/implementation</code></td><td>✅ After a file of that language is opened and server is <code>Running</code></td></tr><tr><td>Project-wide regex symbol search</td><td><code>SemanticIndex</code></td><td>⚫ Disabled (always empty)</td></tr><tr><td><strong>Any project-wide search with no LSP</strong></td><td>—</td><td>❌ Not possible, no fallback</td></tr></tbody></table>`,64)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
